# Host Python deps for the FastAPI shim.
# mt5linux is installed separately in the Dockerfile via --no-deps because
# mt5linux 0.1.9 pins numpy==1.21.4 (no wheel for Python 3.12). Installing
# without its deps and then providing modern rpyc/plumbum/numpy manually
# matches the pattern gmag11 uses.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
httpx==0.28.1

# mt5linux's deps, satisfied at modern versions:
rpyc==5.2.3
plumbum==1.7.0
numpy>=1.26
