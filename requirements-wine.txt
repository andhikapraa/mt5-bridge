# Wine-side Python deps (installed at first boot into the Wine prefix).
# Kept separate from host requirements because they target Python-for-Windows.

MetaTrader5==5.0.36
mt5linux==1.0.3
pywin32==311
