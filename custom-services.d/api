#!/usr/bin/with-contenv bash
# FastAPI shim — translates REST/WebSocket to mt5linux RPyC.
set -euo pipefail

cd /app
exec python3 -m uvicorn api.main:app \
  --host 0.0.0.0 \
  --port "${API_PORT}" \
  --log-level info \
  --no-access-log
