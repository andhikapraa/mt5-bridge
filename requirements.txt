# Host (Linux) Python deps for the FastAPI shim.
# Pinned strict; image build fails loud if any wheel is missing for Python 3.12.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
httpx==0.28.1

# RPyC client to reach the mt5linux server running inside Wine.
# Match the version that mt5linux==1.0.3 ships with on the Wine side.
rpyc==5.2.3
plumbum==1.7.0

# mt5linux client (talks to the Wine-side mt5linux server over RPyC).
mt5linux==1.0.3
